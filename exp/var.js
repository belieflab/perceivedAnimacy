trialIterator = 1;

// (1) define variables
let chase = [];
let noChase = [];
for (let i = 1; i < 300; i++) {
    chase.push(
        "trial"+i+".mp4"
    ) 
    noChase.push(
        "trial"+i+".mp4"
    ) 
}